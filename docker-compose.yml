version: '3.8'

services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: article-sqlserver
    environment:
      SA_PASSWORD: "YourStrong!Passw0rd"
      ACCEPT_EULA: "Y"
    ports:
      - "1433:1433"
    volumes:
      - sqlvolume-article:/var/opt/mssql
    restart: always
  
  sqlserver-comment:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: comment-sqlserver
    environment:
      SA_PASSWORD: "YourStrong!Passw0rd"
      ACCEPT_EULA: "Y"
    ports:
      - "1434:1433"
    volumes:
      - sqlvolume-comment:/var/opt/mssql
    restart: always
  
  sqlserver-profanity:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: profanity-sqlserver
    environment:
      SA_PASSWORD: "YourStrong!Passw0rd"
      ACCEPT_EULA: "Y"
    ports:
      - "1435:1433"
    volumes:
      - sqlvolume-profanity:/var/opt/mssql
    restart: always

  db-init:
    image: mcr.microsoft.com/mssql-tools
    depends_on:
      - sqlserver
    volumes:
      - ./init.sql:/init.sql
    entrypoint: >
      /bin/bash -c "
      sleep 25 &&
      /opt/mssql-tools/bin/sqlcmd -S sqlserver -U sa -P YourStrong!Passw0rd -i /init.sql
      "

  db-comment-init:
    image: mcr.microsoft.com/mssql-tools
    depends_on:
      - sqlserver-comment
    volumes:
      - ./comment-init.sql:/init.sql
    entrypoint: >
      /bin/bash -c "
      sleep 25 &&
      /opt/mssql-tools/bin/sqlcmd -S sqlserver-comment -U sa -P YourStrong!Passw0rd -i /init.sql
      "

  db-profanity-init:
    image: mcr.microsoft.com/mssql-tools
    depends_on:
      - sqlserver-profanity
    volumes:
      - ./profanity-init.sql:/init.sql
    entrypoint: >
      /bin/bash -c "
      sleep 25 &&
      /opt/mssql-tools/bin/sqlcmd -S sqlserver-profanity -U sa -P YourStrong!Passw0rd -i /init.sql
      "

  api-1:
    build: .
    image: happyheaders-api:latest
    ports:
      - "5199:5199"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    depends_on:
      - sqlserver
  
  api-2:
    image: happyheaders-api:latest
    ports:
      - "5200:5199"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    depends_on:
      - sqlserver

volumes:
  sqlvolume-article:
  sqlvolume-comment:
  sqlvolume-profanity: